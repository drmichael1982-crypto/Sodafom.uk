/**
 * Open testing mode keeps every learning game available while the site is
 * being checked. Payment prompts stay hidden, but real sign-in and account
 * creation remain visible so authentication can be tested normally.
 *
 * Set this to false before the paid public launch.
 */
export const OPEN_TESTING_MODE = true;
